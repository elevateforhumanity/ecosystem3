# If you MUST keep a couple of images in repo, you can use LFS (optional):
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# Recommend: store media in Cloudflare R2 instead and link via CDN.
